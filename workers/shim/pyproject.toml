[project]
name = "decomposable-worker"
version = "0.1.0"
description = "JSON-RPC-over-stdio shim shared by every Decomposable Python worker"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["decomposable_worker"]
