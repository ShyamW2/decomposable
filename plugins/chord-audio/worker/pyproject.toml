[project]
name = "decomposable-worker-chord-audio"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "decomposable-worker",
  "librosa>=0.10.2",
  "numpy>=1.24",
  "soundfile>=0.12",
]

[tool.uv.sources]
decomposable-worker = { path = "../../../workers/shim", editable = true }
