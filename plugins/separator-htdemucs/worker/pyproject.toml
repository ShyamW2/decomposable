[project]
name = "musician-worker-separator-htdemucs"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "musician-worker",
  "demucs>=4.0.1",
  "torch==2.2.2",
  "torchaudio==2.2.2",
  "soundfile>=0.12",
  "numpy<2",
]

[tool.uv.sources]
musician-worker = { path = "../../../workers/shim", editable = true }
