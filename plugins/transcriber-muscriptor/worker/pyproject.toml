[project]
name = "decomposable-worker-transcriber-muscriptor"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "decomposable-worker",
  "muscriptor>=0.3",
  "soundfile>=0.12",
]

[tool.uv.sources]
decomposable-worker = { path = "../../../workers/shim", editable = true }
