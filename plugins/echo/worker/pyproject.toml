[project]
name = "decomposable-worker-echo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["decomposable-worker"]

[tool.uv.sources]
decomposable-worker = { path = "../../../workers/shim", editable = true }
