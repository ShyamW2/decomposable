[project]
name = "musician-worker-echo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["musician-worker"]

[tool.uv.sources]
musician-worker = { path = "../../../workers/shim", editable = true }
